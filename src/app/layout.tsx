import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Barcelona Dreaming | Your Soft Landing in Barcelona",
  description: "The complete guide to relocating to Barcelona. Written by a family who made the move. Free guides, checklists, and vetted service providers.",
  keywords: ["moving to barcelona", "barcelona relocation", "expat barcelona", "barcelona guide", "NIE barcelona"],
  openGraph: {
    title: "Barcelona Dreaming | Your Soft Landing in Barcelona",
    description: "The complete guide to relocating to Barcelona. Written by a family who made the move.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${playfair.variable} antialiased min-h-screen flex flex-col`}
      >
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
